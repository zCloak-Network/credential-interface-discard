/*
 * @Description:
 * @Author: lixin
 * @Date: 2022-01-21 13:33:28
 * @LastEditTime: 2022-01-21 13:55:14
 */
import React from "react";
const Tab: React.FC = ({}) => {
  return (
    <div>
      {/* <ul className="menu" onClick={handleClick}>
        {MENU.map((it) => (
          <li
            key={it.key}
            data-id={it.key}
            data-url={it.url}
            className={module === it.key ? "active" : ""}
          >
            {it.title}
          </li>
        ))}
      </ul> */}
    </div>
  );
};
export default Tab;
